# Issue tracker: GitHub

Issues live in the repo's GitHub Issues. The `gh` CLI is used to create, list, and update issues.

## When a skill says "publish to the issue tracker"

Run `gh issue create --title "..." --body "..." --label "..."`.

## When a skill says "fetch the relevant ticket"

Run `gh issue view <number>` or the user provides the URL/number directly.